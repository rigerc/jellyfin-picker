/// Stable route paths shared by app composition and deep-link configuration.
abstract final class AppRoutePaths {
  static const home = '/';
  static const connection = '/connection';
  static const catalog = '/catalog';
}
